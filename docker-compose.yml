version: '3.4'

services:
  cachedb:
    image: redis
    
  jpsage-erp.webapi:
    image: ${DOCKER_REGISTRY-}jpsageerpwebapi
    build:
      context: .
      dockerfile: JPSAGE-ERP.WebAPI/Dockerfile

  cache.api:
    image: ${DOCKER_REGISTRY-}cacheapi
    build:
      context: .
      dockerfile: Cache.API/Dockerfile

